# TEMPLATE ONLY — no real values here, ever.
# Real secrets live in Doppler (project: worker, config: dev).
# Run anything that needs them with:  doppler run -- <command>
#
# These are the variable names the app expects. Track B confirms/extends this list.

SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=     # client-safe (old name: anon key)
SUPABASE_SECRET_KEY=          # SERVER-ONLY (old name: service_role key) — never in the browser
SUPABASE_DB_PASSWORD=
SUPABASE_PROJECT_REF=

# Added later by the tracks (not needed for Deliverable -1):
# ANTHROPIC_API_KEY=          # for the AI assist (separate from your Claude Max login)
# TELEGRAM_BOT_TOKEN=         # Track A — Telegram channel
# WORKER_API_URL=             # CRM -> worker connect/status endpoint
# WORKER_API_JWT_AUDIENCE=    # Supabase JWT verification on the worker
