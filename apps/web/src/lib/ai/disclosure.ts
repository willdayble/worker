// Disclosure shown wherever AI assist is used (CONTRACTS §6 M15 — both directions:
// inbound messages are AI-analyzed; replies are AI-drafted). A human reviews and
// sends every reply; the assistant never auto-sends and never auto-blocks.
export const AI_DISCLOSURE =
  'AI-assisted — inbound messages are analyzed and replies are drafted by AI. A human reviews and sends every reply. Red flags are advisory only; the assistant never auto-sends or auto-blocks.';
